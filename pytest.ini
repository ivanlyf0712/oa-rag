[pytest]
testpaths = tests
# Legacy CorpChat tests target the removed apps.corpchat package and are kept
# for reference only (see tests/legacy_corpchat/README.md). Exclude from the
# default run.
norecursedirs = tests/legacy_corpchat .git venv .scratch
